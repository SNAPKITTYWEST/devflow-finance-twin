; Simple arithmetic and branch
    LOAD_IMM R0, 10
    LOAD_IMM R1, 32
    ADD R2, R0, R1
    LOAD_IMM R3, 42
    CMP R2, R3
    JE equal
    LOAD_IMM R4, 0
    JMP done
equal:
    LOAD_IMM R4, 1
done:
    HALT
